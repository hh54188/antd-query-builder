import {List, Map} from 'immutable'

export const DEFAULT_LIST = new List()

export const DEFAULT_MAP = new Map()

export const DEFAULT_FUNCTION = new Function()

export const DEFAULT_ARRAY = new List()

export const DEFAULT_OBJECT = {}
